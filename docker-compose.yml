services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:3000"
    restart: unless-stopped
    networks:
      - web-network

networks:
  web-network:
    driver: bridge
